// src/components/dashboard/MT5AccountManager.tsx

// ... imports

// This will automatically grab the URL you set in Step 1 or Step 2
const API_BASE_URL =
  process.env.NEXT_PUBLIC_BACKEND_URL ||
  process.env.NEXT_PUBLIC_API_URL ||
  "http://localhost:3001";

// ... rest of the code
